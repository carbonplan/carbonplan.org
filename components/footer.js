/** @jsx jsx */
import { Flex, Box } from 'theme-ui'
import { jsx } from 'theme-ui'

const Footer = () => {
  return (
    <Box
      sx={{
        margin: '0 auto',
        maxWidth: 960,
        padding: '1.45rem 1.0875rem',
        mb: [1]
      }}
    >
      CarbonPlan is a California non-profit public benefit corporation, 
      501(3)(c) status pending 
    </Box>
  )
}

export default Footer
